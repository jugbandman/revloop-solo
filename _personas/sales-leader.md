Sales leader overlay documents the template's default shape. The vault ships sales-leader-ready, so this file is a no-op manifest for sales-leader operators.

# Sales Leader Overlay

This documents the current default behavior of revloop-solo. The template ships sales-leader-shaped out of the box, so this overlay is a no-op for sales-leader operators.

## What ships active for this persona
- Folders: `pipeline/{prospects,deals,closed}/`, `playbooks/`, `knowledge/`
- Skills active: all daily skills + meeting-prep (sales discovery flavor) + icp-review + sales-coach agent auto-loading
- Sacred sources: USER.md, positioning.md, icp.md, brand-voice.md
